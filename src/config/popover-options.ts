import { GlobalPopoverOptions } from './global-popover-options';
/**
 * Options for the popover that will override the global
 *
 * these options are the one provided by the configuration
 * They all can be overwritten by the options injected to a single popover, with component inputs
 */
export class PopoverOptions extends GlobalPopoverOptions {
}
